<%= simple_form_for customer.comments.build do |f| %>
  <%= f.input :body, as: :trix_editor %>
  <%= f.input :customer_id, as: :hidden %>
  <%= f.button :submit, 'Salvar comentário', class: 'btn btn-success margin-top-20'%>
<% end %>
