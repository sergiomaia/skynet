<%= simple_form_for([@customer, @customer.protocols.build]) do |f| %>
	<%= f.input :number %>
	<%= f.input :call_time %>
	<%= f.button :submit, 'Salvar protocolo', class: 'btn btn-success'%>
<% end %>
